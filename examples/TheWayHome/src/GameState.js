def('TWH.GameState', {
    extend: Jxl.State,
    init: function() {
        Jxl.State.prototype.init.call(this);
    },
    update: function() {
        Jxl.State.prototype.update.call(this);
    }
});