def('TWH.MenuState', {
    extend: Jxl.State,
    init: function() {
        Jxl.State.prototype.init.call(this);
        //TODO: Show title w/ clouds passing and giant cat. Start Button
    },
    update: function() {
        Jxl.State.prototype.update.call(this);
    }
});
